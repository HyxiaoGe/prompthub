# PromptHub — 统一提示词管理平台系统设计

## 一、项目定位

PromptHub 是一个面向多 AI 项目团队的**统一提示词管理与智能优化平台**。核心解决三个问题：

1. **提示词碎片化** — 散落在各项目代码中，难以复用和维护
2. **跨项目依赖** — 会议纪要需要调用生图的提示词，视频需要复用生图提示词，依赖关系混乱
3. **缺乏数据驱动** — 不知道哪个提示词效果好，无法持续优化

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     外部业务系统                              │
│   音频摘要系统 ·  AI生图系统 ·  会议纪要系统 ·  AI视频生成     │
└──────────────────────────┬──────────────────────────────────┘
                           │ REST API / GraphQL / SDK
┌──────────────────────────▼──────────────────────────────────┐
│                      API Gateway                             │
│           认证鉴权 · 限流熔断 · 请求路由 · 审计日志            │
├─────────────────────────────────────────────────────────────┤
│                      核心服务层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ 提示词核心引擎 │  │ 组合编排引擎  │  │ 项目空间管理  │       │
│  │ CRUD·版本·模板 │  │ 依赖·继承·DAG │  │ 隔离·共享·权限 │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
├─────────┼─────────────────┼─────────────────┼───────────────┤
│         │           智能层  │                 │               │
│  ┌──────▼───────┐  ┌──────▼───────┐  ┌──────▼───────┐       │
│  │ AI 智能优化   │  │ 数据分析平台  │  │ 个性化引擎    │       │
│  │ 优化·推荐·评估 │  │ 统计·追踪·洞察 │  │ 画像·偏好学习  │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
├─────────┼─────────────────┼─────────────────┼───────────────┤
│         │         基础设施层 │                 │               │
│  ┌──────▼───────┐  ┌──────▼───────┐  ┌──────▼───────┐       │
│  │   存储层      │  │  消息队列     │  │  LLM 网关     │       │
│  │ PG·Redis·S3  │  │ Kafka/RMQ    │  │ 多模型路由     │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心数据模型

### 3.1 提示词（Prompt）

```yaml
Prompt:
  id: uuid
  name: string                    # 提示词名称
  slug: string                    # URL 友好标识
  description: string             # 描述
  content: text                   # 提示词内容（支持模板语法）
  format: enum                    # text | json | yaml | chat
  template_engine: enum           # jinja2 | mustache | none
  variables:                      # 模板变量定义
    - name: string
      type: enum                  # string | number | enum | boolean
      required: boolean
      default: any
      enum_values: string[]       # 当 type=enum 时
  tags: string[]                  # 标签
  category: string                # 分类
  project_id: uuid                # 所属项目
  is_shared: boolean              # 是否发布到共享仓库
  created_by: uuid
  created_at: timestamp
  updated_at: timestamp
```

### 3.2 版本（PromptVersion）

```yaml
PromptVersion:
  id: uuid
  prompt_id: uuid
  version: semver                 # 语义化版本号 1.2.3
  content: text                   # 该版本的完整内容
  variables: json                 # 该版本的变量定义
  changelog: text                 # 变更说明
  status: enum                    # draft | published | deprecated
  created_by: uuid
  created_at: timestamp
```

### 3.3 场景编排（Scene）— ⭐ 核心差异化

```yaml
Scene:
  id: uuid
  name: string                    # 场景名称，如 "会议纪要生图"
  project_id: uuid
  pipeline:                       # 编排管道
    - step: int
      ref: string                 # 引用的提示词 ID 或 slug
      ref_version: string         # 指定版本，"latest" 或具体版本号
      override:                   # 覆写变量
        key: value
      condition: string           # 条件表达式，如 "input.has_chart == true"
      transform: string           # 输出转换，如 "extract_json"
  merge_strategy: enum            # concat | chain | select_best
  separator: string               # 合并分隔符
  output_format: string           # 最终输出格式
```

### 3.4 提示词引用关系（PromptRef）

```yaml
PromptRef:
  id: uuid
  source_prompt_id: uuid          # 引用方
  target_prompt_id: uuid          # 被引用方
  source_project_id: uuid
  target_project_id: uuid         # 跨项目时不同
  ref_type: enum                  # extends | includes | composes
  override_config: json           # 覆写配置
```

---

## 四、核心模块详细设计

### 4.1 组合编排引擎（核心差异化能力）

这是你需求中最独特的部分——解决"会议纪要调用生图提示词"这类跨项目复用问题。

**设计思路：把提示词当作可组合的函数**

```python
# 场景：会议纪要需要生成配图
# 编排引擎的解析流程

def resolve_scene(scene_id, input_vars):
    scene = db.get_scene(scene_id)
    
    resolved_prompts = []
    for step in scene.pipeline:
        # 1. 获取被引用的提示词（可能跨项目）
        prompt = prompt_service.get(step.ref, version=step.ref_version)
        
        # 2. 检查条件是否满足
        if step.condition and not evaluate(step.condition, input_vars):
            continue
        
        # 3. 合并变量：场景覆写 > 输入变量 > 默认值
        merged_vars = {**prompt.defaults, **input_vars, **step.override}
        
        # 4. 渲染模板
        rendered = template_engine.render(prompt.content, merged_vars)
        
        # 5. 可选的输出转换
        if step.transform:
            rendered = apply_transform(rendered, step.transform)
        
        resolved_prompts.append(rendered)
    
    # 6. 按策略合并
    return merge(resolved_prompts, scene.merge_strategy, scene.separator)
```

**依赖关系管理：**

```
DAG 示例：

img_gen_base_v2  ←─── meeting_summary_image（会议纪要生图场景）
       ↑
       └──────── video_promo_thumbnail（视频宣传图场景）
       ↑
style_presets  ←─── meeting_summary_image
                ←── video_promo_thumbnail
```

编排引擎需要：
- 构建 DAG 依赖图
- 检测循环引用
- 版本锁定（避免上游变更意外影响下游）
- 变更通知（上游更新时通知所有下游场景）

### 4.2 API 设计

```yaml
# ===== 提示词管理 =====
POST   /api/v1/prompts                    # 创建提示词
GET    /api/v1/prompts                    # 列表（支持过滤、分页）
GET    /api/v1/prompts/{id}               # 获取详情
PUT    /api/v1/prompts/{id}               # 更新
DELETE /api/v1/prompts/{id}               # 删除（软删除）
GET    /api/v1/prompts/{id}/versions      # 版本历史
POST   /api/v1/prompts/{id}/publish       # 发布新版本

# ===== 场景编排（核心） =====
POST   /api/v1/scenes                     # 创建场景
GET    /api/v1/scenes/{id}                # 获取场景配置
POST   /api/v1/scenes/{id}/resolve        # 🔥 解析场景，返回组装后的提示词
  Body: { variables: {...}, context: {...} }
GET    /api/v1/scenes/{id}/dependencies   # 查看依赖图

# ===== 项目管理 =====
POST   /api/v1/projects                   # 创建项目
GET    /api/v1/projects/{id}/prompts      # 项目下的提示词
POST   /api/v1/projects/{id}/share        # 发布提示词到共享仓库
GET    /api/v1/shared/prompts             # 共享仓库浏览

# ===== 智能优化 =====
POST   /api/v1/optimize                   # 提交优化任务
GET    /api/v1/optimize/{task_id}         # 查看优化结果
POST   /api/v1/evaluate                   # 批量评估
GET    /api/v1/recommendations            # 获取推荐

# ===== 数据分析 =====
GET    /api/v1/analytics/overview         # 全局概览
GET    /api/v1/analytics/trending         # 热门提示词
GET    /api/v1/analytics/prompt/{id}      # 单个提示词的调用统计
GET    /api/v1/analytics/cost             # 成本分析
```

**外部系统调用示例：**

```python
# 会议纪要系统在生成配图时调用 PromptHub
import prompthub

client = prompthub.Client(api_key="xxx", base_url="https://prompthub.internal")

# 方式一：直接获取单个提示词
prompt = client.prompts.get("img_gen_base", version="latest")
rendered = prompt.render(style="商务简约", subject="季度会议总结")

# 方式二：通过场景编排获取组合后的提示词（推荐）
result = client.scenes.resolve(
    "meeting_summary_image",
    variables={
        "subject": "Q4 季度总结会议",
        "style": "商务",
        "has_chart": True
    }
)
final_prompt = result.content  # 已经组装好的最终提示词

# 然后把 final_prompt 传给生图 API
image = dalle.generate(prompt=final_prompt)
```

### 4.3 AI 智能优化模块

```python
# 优化流程

class PromptOptimizer:
    def optimize(self, prompt_id, objective, eval_dataset):
        original = db.get_prompt(prompt_id)
        
        # 1. 分析当前提示词的问题
        analysis = llm.analyze(
            f"分析以下提示词的潜在问题：\n{original.content}\n"
            f"优化目标：{objective}"
        )
        
        # 2. 生成候选变体（3-5个）
        candidates = llm.generate_variants(original.content, analysis, n=5)
        
        # 3. 在评估数据集上跑分
        scores = []
        for candidate in candidates:
            score = self.evaluate(candidate, eval_dataset)
            scores.append(score)
        
        # 4. 选择最优 + 生成对比报告
        best = candidates[argmax(scores)]
        report = self.generate_report(original, best, scores)
        
        return OptimizeResult(
            optimized_prompt=best,
            improvement=scores[argmax(scores)] - baseline_score,
            report=report
        )
```

---

## 五、技术选型建议

| 层次 | 推荐方案 | 备选 |
|------|---------|------|
| 后端框架 | Python FastAPI | Go Gin / Node NestJS |
| 数据库 | PostgreSQL | MySQL 8 |
| 缓存 | Redis | |
| 搜索 | Elasticsearch | Meilisearch |
| 消息队列 | RabbitMQ（中小规模） | Kafka（大规模） |
| 对象存储 | MinIO（自部署）| S3 |
| 模板引擎 | Jinja2 | Mustache |
| 前端 | React + Ant Design | Vue + Element Plus |
| LLM 网关 | LiteLLM | 自建统一网关 |
| 部署 | Docker Compose → K8s | |

---

## 六、分阶段实施路线

### Phase 1 — MVP（4-6 周）
- [ ] 提示词 CRUD + 版本管理
- [ ] 项目空间 + 基础权限
- [ ] RESTful API + Python SDK
- [ ] 简单的 Web 管理界面
- [ ] 基础的模板变量渲染

### Phase 2 — 核心能力（4-6 周）
- [ ] 组合编排引擎 + 场景管理
- [ ] 跨项目引用与共享仓库
- [ ] 依赖图可视化
- [ ] 调用日志与基础统计
- [ ] 对接现有业务系统（音频摘要、生图、视频）

### Phase 3 — 智能化（4-6 周）
- [ ] AI 提示词优化
- [ ] A/B 测试框架
- [ ] 推荐引擎
- [ ] 数据分析看板
- [ ] 成本追踪与告警

### Phase 4 — 规模化（持续迭代）
- [ ] 个性化引擎
- [ ] 团队协作（评审、评论）
- [ ] Webhook + 事件订阅
- [ ] 多语言 SDK（Node / Java / Go）
- [ ] 开放 API Marketplace

---

## 七、与现有开源方案的差异

| 能力 | Langfuse | PromptLayer | Latitude | **PromptHub（本方案）** |
|------|----------|-------------|----------|----------------------|
| 提示词 CRUD + 版本 | ✅ | ✅ | ✅ | ✅ |
| API 获取提示词 | ✅ | ✅ | ✅ | ✅ |
| 调用追踪/监控 | ✅⭐ | ✅ | ✅ | ✅ |
| **跨项目编排/组合** | ❌ | ❌ | 部分 | ✅⭐ 核心能力 |
| **场景化依赖管理** | ❌ | ❌ | ❌ | ✅⭐ 核心能力 |
| AI 自动优化 | ❌ | ❌ | ✅ | ✅ |
| 个性化推荐 | ❌ | ❌ | ❌ | ✅ |
| 热门统计/分析 | 部分 | ✅ | 部分 | ✅ |
